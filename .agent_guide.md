# fast-semver-rs 指南

本层只负责 frozen `semver==3.0.4` 的 opt-in Rust-backed API replacement。
Rust 仅拥有 strict parse；Python 继续拥有公开对象、动态类型、CLI、warnings、
任意大整数 fallback 和精确异常。发行名不能满足 `semver` dependency，禁止声称
resolver-transparent。所有阶段状态以 shared `PIPELINE-STATE.json` 为 SSOT。
